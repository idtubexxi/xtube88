APP_NAME=XTube
APP_ENV=production
APP_KEY=base64:HuhQLR/FvaPh/EPsCx4L/8tMG52giwojyUs8WujTl4E=
APP_DEBUG=false
APP_URL=https://stream.xtubes.site
ASSET_URL=https://stream.xtubes.site

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
PHP_CLI_SERVER_WORKERS=4
BCRYPT_ROUNDS=12

# Logging - IMPORTANT untuk Docker
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Database PostgreSQL
DB_CONNECTION=pgsql
DB_HOST=db.prisma.io
DB_PORT=5432
DB_DATABASE=postgres
DB_USERNAME=f2371dc5983395c54215451b45f0b9b09117cc2abfd6367376831729c17f99af
DB_PASSWORD=sk_t7bcapvo544KXIlBAQRCI
DB_SSLMODE=require

# Session - Database driver lebih stabil untuk Docker
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=.xtubes.site
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=lax

# Broadcasting & Queue
BROADCAST_CONNECTION=log
QUEUE_CONNECTION=database

# Cache - Database untuk production
CACHE_STORE=database
CACHE_PREFIX=laravel_cache

# Filesystem
FILESYSTEM_DISK=public

# Redis - Optional
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=id.tubexxi@gmail.com
MAIL_PASSWORD=lsuplgvrihuiecwg
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="support@xtube.com"
MAIL_FROM_NAME="${APP_NAME}"

# AWS (if needed)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Trusted Proxies - IMPORTANT untuk Nginx reverse proxy
TRUSTED_PROXIES=*
TRUSTED_HOSTS=stream.xtubes.site,*.xtubes.site

# Vite
VITE_APP_NAME="${APP_NAME}"
